// main.jsx
import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'; // ตรวจสอบเส้นทางไฟล์ให้ถูกต้อง

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
